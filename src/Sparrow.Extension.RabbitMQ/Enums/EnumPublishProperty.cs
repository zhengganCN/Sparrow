namespace Sparrow.Extension.RabbitMQ.Enums
{
    /// <summary>
    /// 枚举发布属性
    /// </summary>
    public enum EnumPublishProperty
    {
        content_type,
        content_encoding,
        priority,
        correlation_id,
        reply_to,
        expiration,
        message_id,
        timestamp,
        type,
        user_id,
        app_id,
        cluster_id
    }
}
